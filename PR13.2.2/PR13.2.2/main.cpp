#include <iostream>
#include <cmath>
#include <stdio.h>
#include <algorithm>
#include <vector>
#include <string>

#define DIFFERENCE(a1, a2, b1, b2, f) (set_difference(a1, a2, b1, b2, f))

#define PRINT(str) cout << (str) //вивід

using namespace std;

int main()
{
    const int n = 5;
    const int A[n] = { 1, 2, 3, 4, 5 };
    const int B[n] = { 1, 2, 3, 6, 7 };
    vector <int> final();
    DIFFERENCE(A, A+n, B, B+n, final);

    for (st = v.begin(); st != it; ++st)
            std::cout << ' ' << *st;
        std::cout << '\n';
    
    
    
    for (int i = 1; i <= 5; i++)
    {
        PRINT(w);
    }
}
